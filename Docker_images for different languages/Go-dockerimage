# This is a compiled language 
# Go produces a single binary ,so we usually use multi-stage builds

FROM golang:1.22 AS build

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download 

COPY . . 
RUN CGO_ENABLED=0 GOOS=linux go build -o app

FROM gcr.io/distroless/static-debian12

WORKDIR /app
COPY --from=build /app/app . 
EXPOSE 8080
CMD ["/app/app"]

# flow 
# Compile the code
# Create a binary 
# Copy binary into minimal image 
